apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Ingress được validate cùng application nhưng không làm CD đụng tới Kafka
# hoặc Keycloak.
resources:
  - ../../ingress/api-gateway.yaml
  - ../../ingress/keycloak.yaml
